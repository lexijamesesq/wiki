# Minimal config for the markdownlint pre-commit hook (see
# .pre-commit-config.yaml). Disables only the rule that is genuinely noisy
# on this repo's long prose-style commit-history comments and doc lines;
# every other default markdownlint rule stays on.
MD013: false
